# JianDou 官网

这是 `JianDou` 的 GitHub Pages 官网仓库，使用纯静态 HTML / CSS / JavaScript 构建。

## 本地预览

直接打开 `index.html`，或使用任意静态文件服务器预览。

## 自动部署

推送到 `main` 分支后，`.github/workflows/pages.yml` 会自动部署到 GitHub Pages。
